query getTurns {
  highTurn (where: {status: {_eq: false}}, order_by: {date: desc}) {
    id
    name
    date
    status
  }
  mediumTurn (where: {status: {_eq: false}}, order_by: {date: desc}) {
    id
    name
    date
    status
  }
  lowTurn (where: {status: {_eq: false}}, order_by: {date: desc}) {
    id
    name
    date
    status
  }
}
